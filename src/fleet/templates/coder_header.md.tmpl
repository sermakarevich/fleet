You have been assigned Beads task {task_id}: {task_title}

{task_description}

Task directory: {task_dir}
{invocation_line}
If this task is part of a workflow run, you may publish string values for later steps by writing a flat JSON object to outputs.json in your task directory.
